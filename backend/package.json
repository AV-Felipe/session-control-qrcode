{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend server for session control project",
  "main": "back-server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "AV-Felipe",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3"
  }
}
